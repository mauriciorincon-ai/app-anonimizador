{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts",
    // Los scripts de gate corren en Node puro (sin build) y por eso son .mjs; se incluyen
    // para que `allowJs` les infiera tipos y los tests puedan importarlos con typecheck real.
    "scripts/**/*.mjs",
    // El generador del kit de prueba es el ORÁCULO de los tests del motor: declara qué debería
    // detectar cada columna, así que los tests lo importan y necesita tipos inferidos.
    "docs/kit-de-prueba/*.mjs"
  ],
  "exclude": ["node_modules"]
}
